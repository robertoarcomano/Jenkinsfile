stage("parameters") {
    script {
        def selection = input message: '<message>',
            parameters: [
                run(description: 'Choose a run of the project',
                    filter: 'ALL',
                    name: 'RUN',
                    projectName: 'pipe1'
                )
            ]
        echo "selection is ${selection}"
    }
}